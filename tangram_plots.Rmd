---
title: "TangramsThesis"
output: html_document
---

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
require(tidyr)
require(dplyr)
require(ggplot2)
library(shiny)
```




```{r}
allTangramsFreqs <- read.csv("allTangramsFreqs.csv", header = TRUE)
#change NA values to 0
allTangramsFreqs[is.na(allTangramFreqs)] <- 0

#rearrange data
testTangramDf = spread(allTangramsFreqs, "A", "B")
testTangramDf = gather(allTangramsFreqs, "X1", "X2")
View(testTangramDf)

miniTest= select(allTangramsFreqs, A)
miniTest = slice(miniTest, 1:2)
test = spread(miniTest, A, "walking", "bear")
View(miniTest)
```

```{r}
#plot tangram A, round 1 distribution of word frequencies

A1 <- ggplot(allTangramsFreqs, aes(x = X1)) + geom_dotplot() + ggtitle("A1")
A2 <- ggplot(allTangramsFreqs, aes(x = X3)) + geom_dotplot() + ggtitle("A3")
A3 <- ggplot(allTangramsFreqs, aes(x = X6)) + geom_dotplot() + ggtitle("A6")
B1 <- ggplot(allTangramsFreqs, aes(x = X1.1)) + geom_dotplot() + ggtitle("B1")
multiplot(A1, A2, A3, B1, cols=3, rows =2)
```



```{r}
#http://www.cookbook-r.com/Graphs/Multiple_graphs_on_one_page_(ggplot2)/
# Multiple plot function
#
# ggplot objects can be passed in ..., or to plotlist (as a list of ggplot objects)
# - cols:   Number of columns in layout
# - layout: A matrix specifying the layout. If present, 'cols' is ignored.
#
# If the layout is something like matrix(c(1,2,3,3), nrow=2, byrow=TRUE),
# then plot 1 will go in the upper left, 2 will go in the upper right, and
# 3 will go all the way across the bottom.
#
multiplot <- function(..., plotlist=NULL, file, cols=1, layout=NULL) {
  library(grid)

  # Make a list from the ... arguments and plotlist
  plots <- c(list(...), plotlist)

  numPlots = length(plots)

  # If layout is NULL, then use 'cols' to determine layout
  if (is.null(layout)) {
    # Make the panel
    # ncol: Number of columns of plots
    # nrow: Number of rows needed, calculated from # of cols
    layout <- matrix(seq(1, cols * ceiling(numPlots/cols)),
                    ncol = cols, nrow = ceiling(numPlots/cols))
  }

 if (numPlots==1) {
    print(plots[[1]])

  } else {
    # Set up the page
    grid.newpage()
    pushViewport(viewport(layout = grid.layout(nrow(layout), ncol(layout))))

    # Make each plot, in the correct location
    for (i in 1:numPlots) {
      # Get the i,j matrix positions of the regions that contain this subplot
      matchidx <- as.data.frame(which(layout == i, arr.ind = TRUE))

      print(plots[[i]], vp = viewport(layout.pos.row = matchidx$row,
                                      layout.pos.col = matchidx$col))
    }
  }
}
```

